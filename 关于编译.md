### 关键要点
- 编译 **xiaozhi-esp32** 需要安装 ESP-IDF 环境，克隆项目仓库，并使用特定命令。
- 推荐在 Linux 系统上操作以加快编译速度，Windows 也支持。
- 主要步骤包括设置目标芯片、配置开发板、编译和烧录到设备。

### 环境准备
首先需要安装 ESP-IDF 5.3 或更高版本，推荐使用 Linux 系统（编译更快），但 Windows 也适用。开发工具建议使用 Cursor 或 VSCode，并安装 ESP-IDF 插件。确保项目路径不包含中文字符，以免出现问题。

### 安装教程，直接参考如下即可
https://docs.espressif.com/projects/esp-idf/zh_CN/latest/esp32/get-started/windows-setup.html

https://docs.espressif.com/projects/esp-idf/zh_CN/latest/esp32/get-started/linux-macos-setup.html


### 环境准备, windows需要注意的地方，安装完成后，需要配置下环境变量
直接在安装完成后打开的cmd页面目录下，有配置环境的相关脚本,直接运行就行
```
Done! You can now compile ESP-IDF projects.
Go to the project directory and run:

  idf.py build


D:\Espressif\frameworks\esp-idf-v5.4.1> ipf.py   # 没有配置环境变量，ipf.py命令是不存在的
'ipf.py' 不是内部或外部命令，也不是可运行的程序
或批处理文件。

D:\Espressif\frameworks\esp-idf-v5.4.1>
D:\Espressif\frameworks\esp-idf-v5.4.1>ls
CMakeLists.txt       LICENSE        SECURITY.md           conftest.py  export.ps1    install.sh                tools
COMPATIBILITY.md     README.md      SUPPORT_POLICY.md     docs         export.sh     pytest.ini
COMPATIBILITY_CN.md  README_CN.md   SUPPORT_POLICY_CN.md  examples     install.bat   sdkconfig.rename
CONTRIBUTING.md      ROADMAP.md     add_path.sh           export.bat   install.fish  sgconfig.yml
Kconfig              ROADMAP_CN.md  components            export.fish  install.ps1   sonar-project.properties

```
install.xxx 就是对应终端的相关脚本，
`install.bat`: cmd 终端运行这个
`install.ps1`: powershell 运行这个


运行完成：出现下面提示： 叫你运行当前目录下的export.bat, 直接运行就行，在当前终端下，导入相关idf 编译的环境变量
```
All done! You can now run:
   export.bat

```

```
equirements\requirements.core.txt (line 16)) (0.2.12)
All done! You can now run:
   export.bat

D:\Espressif\frameworks\esp-idf-v5.4.1>export.bat
Activating ESP-IDF 5.4
Setting IDF_PATH to 'D:\Espressif\frameworks\esp-idf-v5.4.1'.
* Checking python version ... 3.11.2
* Checking python dependencies ... OK
* Deactivating the current ESP-IDF environment (if any) ... OK
* Establishing a new ESP-IDF environment ... OK
* Identifying shell ... cmd.exe
* Detecting outdated tools in system ... Found tools that are not used by active ESP-IDF version.
For removing old versions of amazon-corretto-11-x64-windows-jdk, espressif-ide use command 'python.exe D:\Espressif\frameworks\esp-idf-v5.4.1\tools\idf_tools.py uninstall'
To free up even more space, remove installation packages of those tools.
Use option python.exe D:\Espressif\frameworks\esp-idf-v5.4.1\tools\idf_tools.py uninstall --remove-archives.

Done! You can now compile ESP-IDF projects.
Go to the project directory and run:

  idf.py build


D:\Espressif\frameworks\esp-idf-v5.4.1>idf.py build   # 大功告成
Executing action: all (aliases: build)
Running cmake in directory D:\Espressif\frameworks\esp-idf-v5.4.1\build
```

该目录下还有一些简单的例子，在`examples`下，可以尝试一下

为了方便，我们直接把`xianzhi-esp32` 也拷贝到`examples`下, 然后进入该目录
```
cd examples\xiaozhi-esp32
```
根据实际情况，修改好目标芯片参数
编译： 在xiaozhi-esp32根目录下执行 `idf.py build` 即可





### 获取源码和配置
1. **克隆项目**：运行以下命令获取源码：
   ```bash
   git clone https://github.com/78/xiaozhi-esp32
   ```
   或者从 [GitHub](https://github.com/78/xiaozhi-esp32) 下载 ZIP 文件。
2. **设置目标芯片**：根据你的设备选择芯片，例如：
   - ESP32-S3：`idf.py set-target esp32s3`
   - ESP32-C3：`idf.py set-target esp32c3`
3. **配置开发板**：运行 `idf.py menuconfig`，进入配置菜单，在 **Xiaozhi Assistant -> Board Selection** 中选择你的开发板，例如 `bread-compact-wifi` 或 `esp-box-3`。

### 编译和烧录
1. **编译项目**：运行以下命令编译：
   ```bash
   idf.py build
   ```
   为提高编译速度，可先删除 `build` 文件夹，或者关闭杀毒软件。
   若编译失败，检查 ESP-IDF 版本或清理后再试。
   编译成功后，.bin 文件位于 build 文件夹，通常是：
      - build/xiaozhi-esp32.bin（主固件文件）
      - 其他相关文件如 build/bootloader/bootloader.bin 和 build/partition_table/partition-table.bin。
   你可以直接复制 build/xiaozhi-esp32.bin 用于后续用途。


2. **烧录到设备**：编译完成后，使用以下命令烧录并监控输出：
   ```bash
   idf.py build flash monitor
   ```
   - 加速烧录：可使用 `idf.py -b 2000000 build flash monitor`。
   - 指定串口：如果需要，运行 `idf.py -p COM5 build flash monitor`（将 COM5 替换为实际串口，例如 Linux 上的 `/dev/ttyUSB0`）。

### 注意事项
- **串口驱动**：确保已安装 ESP32 芯片的串口驱动。Windows 可在设备管理器查看 COM 端口，Linux 可运行 `ls /dev/ttyUSB*` 检查。
- **常见问题**：
  - 编译失败：检查 ESP-IDF 环境是否正确配置，清理 `build` 文件夹后重试。
  - 烧录失败：确认设备连接正常，进入 bootloader 模式，或检查串口设置。
- **自定义配置**：如需修改唤醒词或 WebSocket URL，可通过 `idf.py menuconfig` 调整：
  - 唤醒词：**ESP Speech Recognition -> Wake Word**
  - WebSocket：**Xiaozhi Assistant -> Websocket -> Websocket URL**

### 参考资料
- [xiaozhi-esp32 GitHub 项目页面](https://github.com/78/xiaozhi-esp32)
- [ESP-IDF 开发环境和编译指南](https://xiaozhi.dev/en/docs/development/esp-idf-setup/)
- [Espressif 官方 ESP-IDF 文档](https://docs.espressif.com/projects/esp-idf/en/latest/get-started/index.html)